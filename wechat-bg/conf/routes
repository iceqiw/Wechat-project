# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Default path will just redirect to the computer list
GET         /index                 controllers.HomeController.index
POST        /register              controllers.UserController.doRegister
POST        /login                 controllers.UserController.doLogin
GET         /notification          controllers.HomeController.notification
POST        /record-student        controllers.StudentController.recordStudentInfo
